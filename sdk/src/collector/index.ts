export { handlePerformance } from './perf' //  性能业务逻辑   环境  行为
export {
  handleBehaviorEvent,
  handleHistoryChange,
  handleHashChange,
} from './behavior'
export { handleEnv } from './env'
export { handleError, handleHttp, handleVueError } from './error'
